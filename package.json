{
  "name": "poring",
  "version": "1.0.0",
  "description": "IRC bot",
  "license": "MIT",
  "author": "Aleksei Parfentjev",
  "type": "module",
  "scripts": {
    "start": "npx tsx src/index.ts",
    "build": "tsc --project tsconfig.json && node scripts/generate-metadata.mjs",
    "check:types": "tsc --noEmit",
    "check:formatting": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "prettier": "^3.9.6",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "pino": "^10.3.1"
  }
}
